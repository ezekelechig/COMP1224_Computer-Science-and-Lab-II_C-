#include <stdio.h>
//#include <math.h>
#include <stdlib.h>
#include <time.h>

int main()

{/*{
    int star,row,rownum,space;
    printf("Type in the number of rows you want to print:\n");
    scanf("%d", &row);

    for(rownum = 1; rownum<=row; rownum++)
    {
       for(space = row-rownum; space >= 1; space--)
       {
           printf(" ");

       }
        for(star=1; star<= (2*rownum)-1; star++)
        {
            printf("*");
        }

    putchar('\n');
    }
    //putchar('\n');

    return 0;
}*/
int main()
{
    int i,n;
    printf("Type the number of rows you want to print: \n");
    scanf("%d",&n)
    for( i = 1; i<=n; i++)
    {
        for(j= i<)
    }
}

